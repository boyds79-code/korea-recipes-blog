# 로컬에서 `npm run generate`를 테스트할 때 사용하세요.
#   set -a && source .env && set +a && npm run generate

ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# 선택: 비워두면 scripts/lib/anthropic.mjs 안의 기본 모델을 사용합니다.
CLAUDE_MODEL=

# 선택: 채우면 scripts/backfill-hero-image.mjs로 대표 이미지를 급하게 채울 때 쓸 수 있습니다
# (기본 자동 생성 파이프라인에서는 더 이상 자동 호출되지 않습니다).
# 무료 발급: https://www.pexels.com/api/
PEXELS_API_KEY=

# 선택: 채우면 레시피마다 필요한 완성/재료 사진 2장을 Gemini가 자동으로 생성해서
# 바로 폴더에 넣어줍니다 (PR에 이미지까지 이미 포함되어 옴 — npm run photos를 안 써도 됨).
# 무료 발급: https://aistudio.google.com/apikey
# 비워두면 예전처럼 AI 이미지 생성 프롬프트만 만들어지고, npm run photos 실행 시
# Bing Image Creator(무료) 링크와 함께 보여줍니다 — 사람이 직접 만들어서 넣으면 됩니다.
GEMINI_API_KEY=

# 선택: 비워두면 scripts/lib/gemini-images.mjs 안의 기본 모델을 사용합니다.
GEMINI_MODEL=

# 선택: 애드센스 승인 후 채우면 광고가 실제로 노출됩니다.
PUBLIC_ADSENSE_CLIENT=
PUBLIC_ADSENSE_SLOT_TOP=
PUBLIC_ADSENSE_SLOT_BOTTOM=
